<body>
	<script type="text/javascript">
		if(location.search.indexOf('reloaded=yes') < 0){
		var hash = window.location.hash;
		var loc = window.location.href.replace(hash, '');
		loc += (loc.indexOf('?') < 0? '?' : '&') + 'reloaded=yes';
		setTimeout(function(){window.location.href = loc + hash;}, 0);
		}
	</script>

	<br>
	<div class="content">
		<%= form_for @document do |d| %>
			Título: <%= d.text_field :name, :class => 'form-control' %>
	    	<br>

	    	<%= d.bootsy_area(:content, class: 'form-control doc-area', :rows => 18) %>

	  		<button type="button" class="btn btn-primary pull-left">Cancelar</button>
	  		<%= d.submit "Salvar alterações", :class => 'btn btn-primary pull-right' %>
	  	<% end %>
	</div>
</body>